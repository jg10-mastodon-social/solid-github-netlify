{
  "name": "solid-github-netlify",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build:config": "node scripts/derive-repo-start-year.mjs",
    "pretest": "npm run build:config",
    "test": "vitest run",
    "test:unit": "vitest run unit",
    "test:integration": "vitest run integration",
    "test:e2e": "vitest run e2e",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^6.0.0",
    "@solid/access-token-verifier": "^2.1.2",
    "mime-types": "^3.0.2",
    "n3": "^1.17.1",
    "prettier": "^3.9.6"
  },
  "devDependencies": {
    "@types/n3": "^1.26.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  }
}
