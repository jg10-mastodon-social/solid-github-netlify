{
  "name": "solid-github-netlify",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@netlify/functions": "^6.0.0",
    "prettier": "^3.9.6"
  }
}
